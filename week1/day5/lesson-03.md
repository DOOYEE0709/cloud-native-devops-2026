# 2-2교시: DB 버전 충돌 관찰하기

## 실습 확인 기록

| 명령/확인 | 결과 |
|---|---|
| | |

## 확인 질문 답변

| 질문 | 답변 |
|---|---|
| 프로젝트 A에서 쓰던 DB 파일을 프로젝트 B DB(상위 버전)로 열었더니 자동 업그레이드 됐다. 다시 A로 돌아갈 수 있을까? | 상황에 따라 어렵거나 불가능하다. DB는 data path를 열 때 버전 포맷으로 자동 업그레이드하는 경우가 있어서, 한 번 올라가면 이전 버전으로 다시 읽을 수 없다. 그래서 실무에서는 버전 비교 테스트를 조심스럽게 한다. |
| 충돌 가능성이 있는 지점은 어디인가? (프로젝트 A: DB 14 / 포트 5432, 프로젝트 B: DB 16 / 포트 5432 or 5433) | port 5432 충돌, data path 혼합, 어느 service가 켜져 있는지 불분명해지는 것. |
| 지우면 안 되는 데이터는 무엇인가? | 각 프로젝트의 data path. 실행 파일은 재설치 가능하지만 data path를 지우면 DB 데이터가 사라진다. |
| 새 컴퓨터에서 다시 만들 때 가장 귀찮은 부분은 무엇인가? | 버전별 설치, 포트 설정, 계정 생성, data path 지정을 다시 맞추는 것. 문서화가 없으면 처음부터 다시 해야 한다. |
| 버전 비교 테스트를 빨리 하려면 무엇이 자동화되어야 하는가? | DB 버전, port, data path, 계정 정보를 명시적으로 선언하고 실행/중지할 수 있어야 한다. → Docker가 이 역할을 한다. |

## notes

### 버전이 두 개가 되면 생기는 문제

DB 하나를 설치하면 실행 파일, service, port, data path, config, account — 이 6가지가 생긴다. 버전이 두 개가 되면 이 요소들이 각각 두 벌이 되거나 서로 섞인다.

| 요소 | 설명 | 중복 설치 시 문제 |
|---|---|---|
| 실행 파일 | DB 서버 프로그램 | 어떤 버전의 명령을 실행하는지 헷갈림 |
| service | 부팅 시 자동 실행되는 백그라운드 프로세스 | 둘 중 무엇이 켜졌는지 모름 |
| port | 클라이언트가 접속하는 번호 | 기본 port 충돌 |
| data path | 실제 데이터 저장 위치 | 프로젝트 데이터 혼합 위험 |
| config file | 인증, 문자셋, port, 경로 | 수정 위치 혼동 |
| account | 접속 인증 정보 | 계정과 권한 문서화 누락 |

### 버전 차이가 왜 문제가 되는가

버전 차이는 단순히 숫자가 아니다. 다음이 달라질 수 있다:
- SQL 문법 지원 범위
- 기본 인증 방식
- 문자셋과 collation 기본값
- index 동작 방식
- 백업/복구 파일 호환성
- driver가 기대하는 protocol

### "그냥 하나 더 설치하면 되지"의 비용

당장은 해결되는 것처럼 보이지만 시간이 지나면 이런 질문이 생긴다:
- 어떤 DB가 지금 켜져 있지?
- 내가 접속한 5432는 어느 프로젝트 DB지?
- 이 데이터 폴더는 지워도 되는 건가?
- 서비스 목록에 남은 이 항목은 뭔가?
- 업그레이드하다가 기존 프로젝트가 깨지면 어떻게 복구하지?

> 학생들이 "귀찮다"라고 느끼는 것이 중요하다. Docker는 바로 이 귀찮음을 줄이기 위해 등장한다. — 강사님

### 관찰 명령

```bash
# Linux/WSL
sudo service postgresql status
psql --version

# macOS
brew services list
psql --version
```

확인할 것:
- 지금 실행 중인 DB 버전은 무엇인가?
- client 버전과 server 버전은 같은가?
- 서비스 이름은 버전을 포함하는가?
- 기본 port는 겹치지 않는가?

### AI 앱과 DB 종류 증가

AI 기능을 붙이면 DB 종류도 늘어난다:

| 용도 | 저장소 |
|---|---|
| 사용자 계정, 결제 정보 | relational DB (PostgreSQL 등) |
| 검색용 문서 임베딩 | vector DB |
| 응답 속도 향상 | cache |
| 작업 대기열 | queue |

AI 앱일수록 "여러 저장소를 빠르게 띄우고 버리고 다시 띄우는 능력"이 필요하다. 모든 걸 OS에 직접 설치하면 충돌과 정리가 어려워진다.

### Week 2 Docker 연결

| 불편함 | Docker에서의 해결 방향 |
|---|---|
| 버전별 설치가 번거로움 | version tag가 붙은 image 사용 |
| service가 OS에 남음 | container 단위로 실행/중지 |
| data path가 섞임 | volume을 명시적으로 분리 |
| port가 겹침 | host port를 다르게 binding |
| 삭제가 불안함 | container/image/volume을 구분해서 정리 |

핵심 문장:
```text
DB 버전 충돌은 DB 프로그램 하나의 문제가 아니라 버전, port, service, data path, config가 함께 얽힌 문제다.
```

## Blocker Log

| 증상 | 확인한 것 |
|---|---|
| | |
